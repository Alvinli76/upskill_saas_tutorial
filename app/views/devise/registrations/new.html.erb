<div class = "container">
  <div class="row">
    <div class = "col-md-4 col-md-offset-4 text-center">
        <div class="well">
            <% if params[:plan] == '2' %>
              <h1> Sign Up for a Pro Account for $10 a month and get special features</h1>
              <%= render "pro_form" %>
              <% else %>
              <h1> Sign up for a basic account</h1>
              <%= render "basic_form" %>
            <% end %>
        
        <%= render "devise/shared/links" %>
        </div>
    </div>
  </div>
</div>
